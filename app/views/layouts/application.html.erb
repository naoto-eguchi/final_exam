<!DOCTYPE html>
<html>
  <head>
    <title>Sample</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  
  <div class="wrap">
    
  <header>
  <div class="container">    
    <ul class="row">
      <% if logged_in? %>
        <li class="col-xs-4 col-sm-4 col-md-4"><%= link_to "Profile", user_path(current_user.id) %></li>
        <li class="col-xs-4 col-sm-4 col-md-4"><%= link_to "Logout", session_path(current_user.id), method: :delete %></li>
        <li class="col-xs-4 col-sm-4 col-md-4"><%= link_to 'ブログ一覧', blogs_path%></li>
    <% else %>
        <li class="col-xs-4 col-sm-4 col-md-4"><%= link_to "Sign up", new_user_path %></li>
        <li class="col-xs-4 col-sm-4 col-md-4"><%= link_to "Login", new_session_path %></li>
    <% end %>
    </ul>
  </div>
  </header>
  
  <body>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>
    
    <%= yield %>
  </body>
  </div>
  
   <footer>
    <p>blogapp</p>
    <p>© Naoto Eguchi</p>
  </footer>
</html>
